name: Deploy (Build Verification)

on:
  push:
    branches:
      - main

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
      - run: pnpm build --filter=@vivero/shared
      - run: pnpm --filter frontend build

  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
      - run: pnpm build --filter=@vivero/shared
      - run: pnpm --filter backend build
