fix(alerts): fix diferencia NaN and add today indicator on siembra date

- Safe Number() parsing with fallback for diferencia calculation
- Red monospace styling for negative difference values
- Add pulsing dot + tooltip on fechaSugeridaSiembra when date is today
- Rename "Nro Contenedor" to "Cantidad" in faltante/falta-pre columns
