fix(users,auth): refine activation workflow and fix layout scroll

- Fix dev accounts query to also filter inactive users
- Toggle between active/pending users DataTable views instead of modal
- Fix auth layout scroll with flex-col and overflow-y-auto
- Use ROUTES.LOGIN constant in register form
- Remove outdated default password messaging
