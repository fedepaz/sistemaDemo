docs(spec): add confirmation dialog form summary design

Design spec for adding explicit form data summary to SlideOverForm
AlertDialog. Uses summaryFields: string[] on ConfirmConfig with
auto-formatting via fieldLabels.
