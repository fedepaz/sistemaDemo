refactor(aSembrar): simplify edit form by removing unused fields

- Remove f_siembra date picker, cantidadGrs, and ajuste fields
- Move cantidaNroCont (Bandejas Confirmadas) to main grid with Cámara de Destino
- Clean up unused imports (Calendar, getLocalDateStr)
